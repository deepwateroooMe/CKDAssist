<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.me.ckdassist">
  
  <uses-permission android:name="android.permission.INTERNET"/>

  <!-- tools:targetApi="31"> -->
  <!-- android:label="@string/app_name" -->
  <!-- android:theme="@style/Theme.CKDAssistApp"> -->
  <!-- android:name=".presentation.BaseApplication" -->
  <!-- android:theme="@style/Theme.CKDAssist"> -->
  <application
      android:name="com.me.ckdassist.presentation.BaseApplication"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/Theme.CKDAssistApp">
    <activity android:name="com.me.ckdassist.presentation.MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>